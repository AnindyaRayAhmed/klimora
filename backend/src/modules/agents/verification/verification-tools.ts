/**
 * Verification tool boundary.
 * TODO: Define tools for signed media reads, metadata reads, and deterministic policy context.
 */
export interface VerificationToolDefinition {
  name: string;
  description: string;
}
