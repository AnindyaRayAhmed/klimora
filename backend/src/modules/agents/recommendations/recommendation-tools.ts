/**
 * Recommendation tool boundary.
 * TODO: Define tools for mission catalog reads, score breakdown reads, and user impact context.
 */
export interface RecommendationToolDefinition {
  name: string;
  description: string;
}
