/**
 * Rit tool boundary.
 * TODO: Define read-only tools for climate facts, forecasts, missions, rankings, and retrieval.
 */
export interface RitToolDefinition {
  name: string;
  description: string;
}
