/**
 * Shared validation boundary.
 * TODO: Add zod schemas for ids, timestamps, coordinates, pagination, and request payloads.
 */
export interface ValidatorDescriptor {
  name: string;
  description: string;
}
