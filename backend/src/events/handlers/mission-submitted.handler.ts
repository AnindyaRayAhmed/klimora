import type { KlimoraEvent } from "../event-types.js";

/**
 * Mission submitted handler.
 * TODO: Start verification workflow after media and metadata are persisted.
 */
export async function handleMissionSubmitted(_event: KlimoraEvent): Promise<void> {}
