import type { FastifyInstance } from "fastify";

/**
 * Forecast route scaffold.
 * TODO: Add forecast summary, warning windows, and provider freshness endpoints.
 */
export async function registerForecastRoutes(_app: FastifyInstance): Promise<void> {}
