import type { FastifyInstance } from "fastify";

/**
 * Community route scaffold.
 * TODO: Add ward, municipality, city ranking, leaderboard, and movement endpoints.
 */
export async function registerCommunityRoutes(_app: FastifyInstance): Promise<void> {}
