import type { FastifyInstance } from "fastify";

/**
 * User route scaffold.
 * TODO: Add profile, impact, preferences, and user-locality endpoints.
 */
export async function registerUserRoutes(_app: FastifyInstance): Promise<void> {}
