import type { FastifyInstance } from "fastify";

/**
 * Auth route scaffold.
 * TODO: Add Supabase session exchange, current-session, and logout helper endpoints.
 */
export async function registerAuthRoutes(_app: FastifyInstance): Promise<void> {}
