import type { FastifyInstance } from "fastify";

/**
 * Environmental map layer route scaffold.
 * TODO: Add layer metadata, tile manifests, and locality overlay endpoints.
 */
export async function registerLayerRoutes(_app: FastifyInstance): Promise<void> {}
